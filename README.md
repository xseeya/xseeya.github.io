# Персональный сайт-визитка с музыкальным плеером

Современный минималистичный персональный сайт с интегрированным музыкальным плеером, поддержкой синхронизированных текстов (LRC формат) и переключением светлой/темной темы.

## ✨ Особенности

- 🎵 **Полнофункциональный музыкальный плеер** с поддержкой MP3, WAV, WebM
- 📝 **Синхронизация текстов песен** в формате LRC
- 🌓 **Светлая и темная темы** с сохранением выбора пользователя
- 📊 **Визуализация аудио** в реальном времени
- 📱 **Адаптивный дизайн** для всех устройств
- ♿ **Доступность** с ARIA-метками и клавиатурной навигацией
- 🚀 **Готов к развертыванию** на GitHub Pages

## 🎮 Функционал плеера

- ▶️ Play/Pause
- ⏭️ Next/Previous track
- 🔀 Shuffle mode
- 🔁 Repeat modes (none, one, all)
- 🔊 Volume control
- ⏱️ Progress bar с возможностью перемотки
- 📋 Playlist с выделением активного трека

## 📄 Работа с текстами песен

Плеер поддерживает формат LRC (Lyric file format):
- Автоматическая синхронизация текста с музыкой
- Подсветка активной строки
- Клик по строке для перехода к этому моменту в треке
- Плавная прокрутка к активной строке

### Формат LRC файла:

```lrc
[ti:Название песни]
[ar:Исполнитель]
[al:Альбом]
[00:12.50]Первая строка текста
[00:16.80]Вторая строка текста
```

## 🚀 Развертывание на GitHub Pages

### Вариант 1: Прямое развертывание (рекомендуется)

1. Создайте репозиторий на GitHub
2. Загрузите все файлы в ветку `gh-pages` или `main`
3. Перейдите в Settings → Pages
4. Выберите ветку для публикации
5. Сайт будет доступен по адресу: `https://username.github.io/repository-name/`

### Вариант 2: Автоматический деплой через GitHub Actions

Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
          publish_branch: gh-pages
```

## 📁 Структура проекта

```
/
├── index.html              # Главная страница
├── playlist.json           # Конфигурация плейлиста
├── css/
│   └── styles.css          # Стили и темы
├── js/
│   ├── app.js              # Основная логика приложения
│   ├── audioPlayer.js      # Класс аудио плеера
│   └── lrcParser.js        # Парсер LRC файлов
├── assets/
│   └── avatar.svg          # Аватар профиля
├── music/                  # Папка для аудиофайлов
│   └── *.mp3
└── lyrics/                 # Папка для LRC файлов
    └── *.lrc
```

## 🎵 Добавление своей музыки

1. Поместите аудиофайлы в папку `music/`
2. Создайте LRC файлы в папке `lyrics/` (опционально)
3. Отредактируйте `playlist.json`:

```json
[
    {
        "title": "Название трека",
        "artist": "Исполнитель",
        "file": "music/your-track.mp3",
        "lyrics": "lyrics/your-track.lrc"
    }
]
```

## ✏️ Персонализация

### Информация о владельце

Отредактируйте в `index.html`:
- Имя (`.name`)
- Возраст (`.age`)
- Описание (`.description`)
- Ссылки на социальные сети (`.social-links`)
- Аватар (`assets/avatar.svg`)

### Цветовая схема

Измените CSS переменные в `css/styles.css`:

```css
:root {
    --accent-primary: #6366f1;    /* Основной акцент */
    --accent-secondary: #8b5cf6;  /* Вторичный акцент */
    /* ... */
}
```

## 🛠️ Технологии

- HTML5
- CSS3 (Custom Properties, Grid, Flexbox)
- JavaScript (ES6+ Modules)
- Web Audio API
- Canvas API

## 📱 Поддержка браузеров

- Chrome/Edge 90+
- Firefox 88+
- Safari 14+
- Opera 76+

## 📝 Лицензия

MIT License - используйте свободно для личных и коммерческих проектов.

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.

---

Сделано с ❤️ и музыкой
