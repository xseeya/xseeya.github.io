# Быстрый старт 🚀

## За 5 минут до запуска

### Шаг 1: Получить проект (30 секунд)

```bash
# Клонировать репозиторий
git clone https://github.com/username/repository-name.git
cd repository-name

# Или скачать ZIP и распаковать
```

### Шаг 2: Добавить музыку (2 минуты)

1. Поместите MP3 файлы в папку `music/`
2. Опционально: добавьте LRC файлы в `lyrics/`
3. Отредактируйте `playlist.json`:

```json
[
    {
        "title": "Название песни",
        "artist": "Исполнитель",
        "file": "music/song.mp3",
        "lyrics": "lyrics/song.lrc"
    }
]
```

### Шаг 3: Персонализировать (2 минуты)

Откройте `index.html` и измените:

```html
<!-- Ваше имя -->
<h1 class="name">Ваше Имя</h1>

<!-- Возраст -->
<p class="age">XX лет</p>

<!-- Описание -->
<p class="description">
    Ваше описание...
</p>

<!-- Социальные сети -->
<a href="https://vk.com/yourprofile">...</a>
<a href="https://t.me/yourusername">...</a>

<!-- Аватар -->
<img src="assets/your-photo.jpg" alt="Аватар профиля" class="avatar">
```

### Шаг 4: Запустить локально (30 секунд)

**Вариант A: Python**
```bash
python -m http.server 8000
# Откройте http://localhost:8000
```

**Вариант B: VS Code**
1. Установите расширение "Live Server"
2. Правый клик на `index.html` → "Open with Live Server"

**Вариант C: Node.js**
```bash
npx http-server
# Откройте http://localhost:8080
```

### Шаг 5: Деплой на GitHub Pages (5 минут)

1. Создайте репозиторий на GitHub
2. Загрузите файлы:
```bash
git add .
git commit -m "Initial commit"
git push origin main
```
3. Settings → Pages → Source: main branch
4. Готово! Сайт доступен на `https://username.github.io/repository-name/`

---

## Минимальный пример

Если хотите просто протестировать функционал без музыки:

1. Откройте `index.html` в браузере через HTTP сервер
2. Плеер будет пытаться загрузить файлы из `playlist.json`
3. Если файлов нет - просто добавьте свои или замените пути на тестовые

### Тестовые аудиофайлы

Можете использовать бесплатные аудио для теста:
- https://freemusicarchive.org/ (свободная музыка)
- https://incompetech.com/ (роялти-фри музыка)
- https://ccmixter.org/ (Creative Commons музыка)

---

## Чеклист готовности к деплою

✅ **Обязательно:**
- [ ] Изменено имя и описание в `index.html`
- [ ] Добавлены музыкальные файлы в `music/`
- [ ] Обновлен `playlist.json` с вашими треками
- [ ] Заменен аватар в `assets/`
- [ ] Обновлены ссылки на соцсети

✅ **Рекомендуется:**
- [ ] Созданы LRC файлы для текстов
- [ ] Изменена цветовая схема (опционально)
- [ ] Добавлен favicon
- [ ] Проверено на мобильных устройствах
- [ ] Сжаты аудиофайлы до разумного размера

✅ **Для GitHub Pages:**
- [ ] Репозиторий создан на GitHub
- [ ] Файлы загружены в ветку main/gh-pages
- [ ] GitHub Pages включен в настройках
- [ ] Сайт открывается без ошибок

---

## Структура файлов (минимум)

```
your-site/
├── index.html          ← Главная страница
├── playlist.json       ← Конфигурация плейлиста
├── css/
│   └── styles.css      ← Стили
├── js/
│   ├── app.js          ← Логика приложения
│   ├── audioPlayer.js  ← Плеер
│   └── lrcParser.js    ← Парсер текстов
├── assets/
│   └── avatar.svg      ← Ваше фото
├── music/
│   └── *.mp3           ← Ваши треки
└── lyrics/
    └── *.lrc           ← Тексты песен (опционально)
```

---

## Распространенные ошибки

### ❌ Ошибка: "Cannot use import statement outside a module"

**Решение:** Используйте HTTP сервер, не открывайте файл напрямую через `file:///`

### ❌ Ошибка: 404 на аудиофайлы

**Решение:** Проверьте пути в `playlist.json`, они должны быть относительными: `"music/song.mp3"`

### ❌ Сайт не обновляется на GitHub Pages

**Решение:** 
1. Подождите 5-10 минут
2. Очистите кэш браузера (Ctrl+Shift+R)
3. Проверьте, что выбрана правильная ветка в Settings → Pages

### ❌ Музыка не воспроизводится

**Решение:**
1. Проверьте консоль браузера (F12)
2. Убедитесь, что файлы существуют по указанным путям
3. Проверьте формат файлов (MP3, OGG, WAV)
4. Размер файлов не должен превышать 100MB для GitHub

---

## Полезные команды

### Проверка синтаксиса JavaScript
```bash
node --check js/app.js
node --check js/audioPlayer.js
node --check js/lrcParser.js
```

### Проверка JSON
```bash
python -m json.tool playlist.json
```

### Сжатие аудио
```bash
# Требует FFmpeg
ffmpeg -i input.mp3 -b:a 128k output.mp3
```

### Локальный сервер (разные варианты)
```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000

# PHP
php -S localhost:8000

# Node.js (без установки)
npx http-server

# Node.js (с установкой)
npm install -g http-server
http-server
```

---

## Что дальше?

После запуска сайта вы можете:

1. **Кастомизировать дизайн** - см. `CUSTOMIZATION.md`
2. **Добавить больше функций** - форкните и модифицируйте код
3. **Оптимизировать производительность** - сожмите файлы
4. **Настроить домен** - см. `DEPLOYMENT.md`
5. **Поделиться с друзьями** - отправьте ссылку!

---

## Нужна помощь?

- 📖 Полная документация: `README.md`
- 🚀 Инструкции по деплою: `DEPLOYMENT.md`
- 🎨 Настройка внешнего вида: `CUSTOMIZATION.md`
- ❓ Частые вопросы: `FAQ.md`
- 🐛 Проблемы? Создайте issue на GitHub

---

**Удачи! 🎵 Создайте что-то крутое!**
